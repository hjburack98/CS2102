
public class HashTables {
	
	/*
	 * Purpose of Hash:
	 * allows extremely fast lookup given a key value
	 * reduce the address space of the information to the table space of the hash table
	 * 
	 * Hash Function:
	 * The reduce function
	 * 
	 * Collision: hash(a) == hash(b) but a!=b
	 * 
	 * Chaining is more general solution to collision
	 * 
	 */

}
